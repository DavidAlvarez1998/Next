// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgur.com'], // Añade los dominios de los que quieres cargar imágenes
    },
};

export default nextConfig;
